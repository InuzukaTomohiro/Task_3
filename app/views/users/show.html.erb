    <div class="col-md-8 offset-md-1">
      <h2>Book</h2>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @Users.each do |user| %>
          <tr>
            <td><%= image_tag "no_image.jpeg" %></td>
            <td><%= link_to user.title, book_path(books.id) %></td>
            <td>aaa<%= user.body %></td>
            <% end %>


          </tr>
        </tbody>
      </table>
    </div>